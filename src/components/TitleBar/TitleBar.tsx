import { Container, Header, Title } from "./TitleBar.styled";
const TitleBar = () => {
  return (
    <Header>
      <Container>
        <Title>User Management Table</Title>
      </Container>
    </Header>
  );
};

export default TitleBar;
